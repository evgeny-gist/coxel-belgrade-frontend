import { Heading, Container } from "@chakra-ui/react";

export const MainPage = () => {
    return (
        <Container w="100%">
            <Heading>Приёмная</Heading>
        </Container>
    );
};
